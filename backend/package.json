{
  "name": "backend",
  "private": true,
  "scripts": {
    "dev": "concurrently -k -n api,worker \"python -m uvicorn --app-dir src chiron_backend.api.main:app --reload --host 0.0.0.0 --port 8000\" \"python run_worker.py\"",
    "build": "python scripts/export_openapi.py ../packages/contracts/openapi.json",
    "worker": "python run_worker.py",
    "lint": "python -m ruff check src tests scripts run_worker.py",
    "format": "python -m ruff format src tests scripts run_worker.py",
    "format:check": "python -m ruff format --check src tests scripts run_worker.py",
    "test": "python -m pytest",
    "typecheck": "python -m mypy src",
    "generate-contracts": "python scripts/export_openapi.py ../packages/contracts/openapi.json"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
