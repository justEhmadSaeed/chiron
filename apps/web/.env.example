# Optional. Base URL of the Chiron FastAPI server (no trailing slash).
# Leave unset for local dev to use the Vite dev server proxy (same-origin /api and /ws).
# VITE_BACKEND_URL=http://127.0.0.1:8000
