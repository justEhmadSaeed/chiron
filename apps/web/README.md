
  # Chiron Web App

  This is the frontend code bundle for the Chiron application.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  